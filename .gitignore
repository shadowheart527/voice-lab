# Build output
engine/build*/
browser/dist/
browser/node_modules/

# Large third-party payloads, fetched by scripts/bootstrap.sh
engine/external/libtorch/
ml/models/

# Python
__pycache__/
*.pyc
.venv*/

# Runtime
*.log
*.wav
!docs/**/*.wav
.DS_Store

# Fetched by scripts/bootstrap.sh, not distributed here
browser/vendor/
browser/models/
engine/external/fftw-wasm/
engine/external/eigen/
.wasm-build/

# browser/wasm/ IS committed: GitHub Pages cannot cross-compile Emscripten and
# FFTW, so the built module ships with the repo and the deployed site is
# guaranteed to be the artifact that was tested. Rebuild with scripts/build-wasm.sh.
